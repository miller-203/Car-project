# Application
spring.application.name=car-rental-backend
server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/carrental
spring.datasource.username=carrental_user
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
# spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# JWT
jwt.secret=mySecretKey123456789012345678901234567890
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.carrental=DEBUG

# WhatsApp notifications (Meta WhatsApp Cloud API)
app.notifications.whatsapp.enabled=false
app.notifications.whatsapp.admin-number=+212 0619797080
app.notifications.whatsapp.phone-number-id=
app.notifications.whatsapp.access-token=
